# Parley loop harness — active sprint scope for the pre-commit sentinel
# and TDD phase for the verification gate.
#
# 'sprint:' selects which scope-<N> regex is enforced. 'phase:' is red (tests
# must fail) or green (tests must pass). Only the HUMAN operator edits this
# file — agents never advance the sprint or flip the phase.
#
# Only the scope-<N> line matching the active 'sprint:' is ever read; the
# earlier lines are kept as the template lineage each sprint is derived from.
# They were path-normalized when the repository layout was reorganized
# (.parley_sprint_scope -> .parley/scope, SPRINT<N>-NOTES.md ->
# docs/sprints/sprint-<NN>-notes.md, which the 'docs/' prefix already covers).
sprint: 23
phase: red
scope-0: ^(src/compat/|src/domain/|tests/|scripts/|docs/|\.parley/|\.githooks/|\.github/|\.gitignore$|AGENTS\.md$|README\.md$|LICENSE$|CONTRIBUTING\.md$)
scope-1: ^(src/compat/|src/domain/|src/manifest/|tests/|scripts/|docs/|\.parley/|\.githooks/|\.github/|\.gitignore$|AGENTS\.md$|README\.md$|LICENSE$|CONTRIBUTING\.md$)
scope-2: ^(src/compat/|src/domain/|src/manifest/|src/resolver/|tests/|scripts/|docs/|\.parley/|\.githooks/|\.github/|\.gitignore$|AGENTS\.md$|README\.md$|LICENSE$|CONTRIBUTING\.md$)
scope-3: ^(src/compat/|src/domain/|src/manifest/|src/resolver/|tests/|scripts/|docs/|\.parley/|\.githooks/|\.github/|\.gitignore$|AGENTS\.md$|README\.md$|LICENSE$|CONTRIBUTING\.md$)
scope-4: ^(src/compat/|src/domain/|src/manifest/|src/resolver/|src/source/|tests/|scripts/|docs/|\.parley/|\.githooks/|\.github/|\.gitignore$|AGENTS\.md$|README\.md$|LICENSE$|CONTRIBUTING\.md$)
scope-5: ^(src/compat/|src/domain/|src/manifest/|src/resolver/|src/source/|src/install/|tests/|scripts/|docs/|\.parley/|\.githooks/|\.github/|\.gitignore$|AGENTS\.md$|README\.md$|LICENSE$|CONTRIBUTING\.md$)
scope-6: ^(src/compat/|src/domain/|src/manifest/|src/resolver/|src/source/|src/install/|src/exec/|bin/|tests/|scripts/|docs/|\.parley/|\.githooks/|\.github/|\.gitignore$|AGENTS\.md$|README\.md$|LICENSE$|CONTRIBUTING\.md$)
# scope-7 additionally admits the one-off migration paths retired by the
# layout reorganization: the old harness-state filename and the flat sprint
# notes. Both appear only as deletions in the reorganization commit and can be
# dropped from scope-8 onward.
scope-7: ^(src/compat/|src/domain/|src/manifest/|src/resolver/|src/source/|src/install/|src/exec/|src/publish/|bin/|tests/|scripts/|docs/|\.parley/|\.githooks/|\.github/|\.gitignore$|AGENTS\.md$|README\.md$|LICENSE$|CONTRIBUTING\.md$|\.parley_sprint_scope$|SPRINT[0-9]+-NOTES\.md$)
# scope-8 drops scope-7's one-off migration paths (the old harness-state
# filename and the flat sprint notes) — both landed as deletions in the
# reorganization commit and can never appear again. No new src/ directory
# this sprint: CommandLine lives in src/exec/, so scripts/run-tests.st is
# unchanged. bin/ stays in scope — the wrapper is rewritten and, for the
# first time, law-guarded.
scope-8: ^(src/compat/|src/domain/|src/manifest/|src/resolver/|src/source/|src/install/|src/exec/|src/publish/|bin/|tests/|scripts/|docs/|\.parley/|\.githooks/|\.github/|\.gitignore$|AGENTS\.md$|README\.md$|LICENSE$|CONTRIBUTING\.md$)
# scope-9 is identical to scope-8: Sprint 9 introduces no new src/
# directory (LockError lives in src/exec/), so scripts/run-tests.st is
# unchanged. bin/ stays in scope — the shipped binary is law-guarded
# from Sprint 8 onward, and wrap-sprint.sh now stages it.
scope-9: ^(src/compat/|src/domain/|src/manifest/|src/resolver/|src/source/|src/install/|src/exec/|src/publish/|bin/|tests/|scripts/|docs/|\.parley/|\.githooks/|\.github/|\.gitignore$|AGENTS\.md$|README\.md$|LICENSE$|CONTRIBUTING\.md$)
# scope-10 is identical to scope-9: Sprint 10 introduces no new src/
# directory and no new class at all. Retirement is a schema plus
# accessors on the settled IndexEntryReader / DirectorySource /
# IndexSnapshot; the lock body-shape check lands in the settled CLI's
# existing pinnedResolution boundary; the boundary table is a class-side
# method on Sprint 8's CommandLine. So scripts/run-tests.st is
# unchanged. bin/ stays in scope — S12 spawns the shipped binary.
scope-10: ^(src/compat/|src/domain/|src/manifest/|src/resolver/|src/source/|src/install/|src/exec/|src/publish/|bin/|tests/|scripts/|docs/|\.parley/|\.githooks/|\.github/|\.gitignore$|AGENTS\.md$|README\.md$|LICENSE$|CONTRIBUTING\.md$)
# scope-11 is identical to scope-10: Sprint 11 introduces no new src/
# directory — SchemaShape lives in src/manifest/ and the three
# inspection verbs live in the settled CLI — so scripts/run-tests.st is
# unchanged. bin/ stays in scope (the binary is law-guarded from Sprint
# 8 onward) even though no Sprint 11 scenario spawns it.
scope-11: ^(src/compat/|src/domain/|src/manifest/|src/resolver/|src/source/|src/install/|src/exec/|src/publish/|bin/|tests/|scripts/|docs/|\.parley/|\.githooks/|\.github/|\.gitignore$|AGENTS\.md$|README\.md$|LICENSE$|CONTRIBUTING\.md$)
# scope-12 is identical to scope-11: Sprint 12 introduces no new src/
# directory and no new class at all. Selective update is an accessor on
# the settled IndexSnapshot plus a verb branch in the settled CLI, so
# scripts/run-tests.st is unchanged. bin/ stays in scope (the binary is
# law-guarded from Sprint 8 onward) even though no Sprint 12 scenario
# spawns it.
scope-12: ^(src/compat/|src/domain/|src/manifest/|src/resolver/|src/source/|src/install/|src/exec/|src/publish/|bin/|tests/|scripts/|docs/|\.parley/|\.githooks/|\.github/|\.gitignore$|AGENTS\.md$|README\.md$|LICENSE$|CONTRIBUTING\.md$)
# scope-13 is identical to scope-12: Sprint 13 introduces no new src/
# directory — Parley.ManifestEditor lives in src/manifest/, which
# scripts/run-tests.st already files in — so that script is unchanged.
# The add verb and the shared deleted-held-pin diagnosis land in the
# settled CLI. bin/ stays in scope (the binary is law-guarded from
# Sprint 8 onward) even though no Sprint 13 scenario spawns it.
scope-13: ^(src/compat/|src/domain/|src/manifest/|src/resolver/|src/source/|src/install/|src/exec/|src/publish/|bin/|tests/|scripts/|docs/|\.parley/|\.githooks/|\.github/|\.gitignore$|AGENTS\.md$|README\.md$|LICENSE$|CONTRIBUTING\.md$)
# scope-14 is identical to scope-13: Sprint 14 introduces no new src/
# directory — Parley.SparseIndexSource lives in src/source/, which
# scripts/run-tests.st already files in — so that script is unchanged.
# Two declared settled-class exceptions this sprint: IndexEntryReader
# (formatTags gains #'parley-listing') and CommandLine (the --index
# sourceFlags row, the fourth deserializationBoundaries row, and the
# usage flags: lines). bin/ stays in scope (the binary is law-guarded
# from Sprint 8 onward) even though no Sprint 14 scenario spawns it.
scope-14: ^(src/compat/|src/domain/|src/manifest/|src/resolver/|src/source/|src/install/|src/exec/|src/publish/|bin/|tests/|scripts/|docs/|\.parley/|\.githooks/|\.github/|\.gitignore$|AGENTS\.md$|README\.md$|LICENSE$|CONTRIBUTING\.md$)
# scope-15 is identical to scope-14: Sprint 15 introduces no new src/
# directory — the publish layouts and --version land in src/exec/ and
# src/publish/, and Parley.Parley (the version constant) lives in
# src/manifest/, all of which scripts/run-tests.st already files in, so
# that script is unchanged. Declared settled-class exceptions this
# sprint: CommandLine (publishLayouts, --version, the fifth
# deserializationBoundaries row, the usage lines), Publisher (the sparse
# layout and the listing write), CLI (the --layout argv pass-through and
# the decision-52 seedNames narrowing) and Parley.Parley (the version
# constant). bin/ stays in scope — the wrapper is law-guarded from
# Sprint 8 onward and S-level scenarios spawn it for --version.
#
# docs/method/ is NEW as of 3e4cd78. It is inside this regex because
# docs/ always was — but it is NOT agent-writable. docs/method/
# findings.md records what the PIPELINE got right and wrong, and you
# are the subject of most of its entries as well as having no
# cross-sprint memory to write them from; the human operator writes
# it at Gate B. The pre-commit sentinel enforces paths, not
# authorship, so it cannot express this and wrap-sprint.sh refuses
# the wrap instead: staging anything under docs/method/ aborts with
# "docs/method/ is operator-authored; unstage it." That refusal is
# the rule working, not a harness bug — unstage and continue. If you
# think a finding belongs in there, write it in the sprint notes or
# say so on the issue and let the operator lift it. Everything else
# under docs/ is yours as always.
scope-15: ^(src/compat/|src/domain/|src/manifest/|src/resolver/|src/source/|src/install/|src/exec/|src/publish/|bin/|tests/|scripts/|docs/|\.parley/|\.githooks/|\.github/|\.gitignore$|AGENTS\.md$|README\.md$|LICENSE$|CONTRIBUTING\.md$)
# scope-16 is identical to scope-15: Sprint 16 introduces no new src/
# directory and no new class at all. All four fixes land on the settled
# CLI and CommandLine, both in src/exec/, which scripts/run-tests.st
# already files in, so that script is unchanged. Declared settled-class
# exceptions this sprint: CLI (the exec script precondition, the lock
# readability check, and the checked held-pins line on add and
# update <pkg>) and CommandLine (--help/-h/help recognized beside the
# settled --version). bin/ stays in scope — the shipped binary is
# law-guarded from Sprint 8 onward and S8-S10 spawn it for --help.
#
# docs/method/ remains operator-authored and is NOT agent-writable,
# exactly as scope-15 records: the pre-commit sentinel enforces paths
# rather than authorship, so wrap-sprint.sh refuses the wrap instead
# when anything under docs/method/ is staged. That refusal is the rule
# working. Everything else under docs/ is the agent's as always.
scope-16: ^(src/compat/|src/domain/|src/manifest/|src/resolver/|src/source/|src/install/|src/exec/|src/publish/|bin/|tests/|scripts/|docs/|\.parley/|\.githooks/|\.github/|\.gitignore$|AGENTS\.md$|README\.md$|LICENSE$|CONTRIBUTING\.md$)
# scope-17 is identical to scope-16: Sprint 17 introduces no new src/
# directory — Parley.PathGuard lives in src/compat/, which
# scripts/run-tests.st already files in first — so that script is
# unchanged. Declared settled-class exceptions this sprint are the
# EIGHT-CLASS I/O CENSUS (§8 decision 60, ruled at the Gate C
# amendment and replacing an earlier five-class list that could not
# satisfy the sprint's own prohibition law): CLI, Publisher,
# SparseIndexSource, ContentStore, ManifestFile, DirectorySource,
# GitIndexSource and CommandLine — every class in src/ performing
# direct File/FileStream/Directory I/O — each bounded to routing that
# I/O through PathGuard and adding its diagnosis, never changing what
# the verb does when the path is fine. ManifestEditor and
# ManifestBuilder are NOT exceptions: neither performs file I/O (the
# add write boundary is in CLI), and needing either is a halt-and-ask.
# bin/ stays in scope — the shipped binary is law-guarded from Sprint 8
# onward, S14 spawns it once per probed state, and S18-S20 rewrite the
# wrapper itself (§8 decision 66).
#
# docs/method/ remains operator-authored and is NOT agent-writable, as
# scope-15 and scope-16 record: the sentinel enforces paths rather than
# authorship, so wrap-sprint.sh refuses the wrap when anything under
# docs/method/ is staged. That refusal is the rule working.
scope-17: ^(src/compat/|src/domain/|src/manifest/|src/resolver/|src/source/|src/install/|src/exec/|src/publish/|bin/|tests/|scripts/|docs/|\.parley/|\.githooks/|\.github/|\.gitignore$|AGENTS\.md$|README\.md$|LICENSE$|CONTRIBUTING\.md$)
# scope-18 is identical to scope-17: Sprint 18 introduces no new src/
# directory — Parley.ManifestCapture lives in src/manifest/, which
# scripts/run-tests.st already files in AND files in BEFORE src/exec/,
# where its one caller lives — so that script is unchanged and must not
# be edited.
#
# Declared settled-class exceptions this sprint are TWO, both in
# src/exec/, and the list is deliberately shorter than the staging
# plan's four (checked against the code at Gate C rather than taken
# from the plan — the scope-17 lineage above records what happens when
# a comment and an issue disagree about this list):
#
#   ManifestFile - wraps step 2's namespace swap and FileStream fileIn:
#     in a ManifestCapture block, prepends the decision-44 batch header
#     naming the path to the authoring problems, and composes the
#     re-voiced file-and-line diagnosis from the capture.
#   CommandLine  - ensureDirectory: distinguishes an unwritable parent
#     from a path of the wrong type (§8 decision 74, method finding
#     F13), and the state-root boundary row gains its declared sentence
#     for the new state.
#
# NOT exceptions, and each is a halt-and-ask if the agent needs it:
# Parley.Parley (the gateway's two handlers are unchanged; the capture
# wraps the file-in in ManifestFile, NOT in the gateway and NOT inside
# ManifestCapture - §8 decision 61), ManifestBuilder (its four problem
# literals stay byte-identical; the header is prepended by ManifestFile),
# ManifestError and CommandLine's batchedErrorClasses (problems already
# become the lines, so the header is a problem entry rather than a new
# mechanism), and CLI (it already passes the path to ManifestFile load:).
#
# THE CENSUS STAYS AT EIGHT. ManifestCapture performs no file I/O; if
# its source acquires 'FileStream ', 'File name:', 'Directory create:'
# or 'Directory workingName' it becomes a ninth class and S13 of the
# settled Sprint 17 suite fails by construction (§8 decisions 60, 61).
#
# bin/ stays in scope — the shipped binary is law-guarded from Sprint 8
# onward, and this sprint's marker-absence law spawns it to capture
# stdout AND stderr, which is the whole point of the law.
#
# docs/method/ remains operator-authored and is NOT agent-writable, as
# scope-15 through scope-17 record: the sentinel enforces paths rather
# than authorship, so wrap-sprint.sh refuses the wrap when anything
# under docs/method/ is staged. That refusal is the rule working.
scope-18: ^(src/compat/|src/domain/|src/manifest/|src/resolver/|src/source/|src/install/|src/exec/|src/publish/|bin/|tests/|scripts/|docs/|\.parley/|\.githooks/|\.github/|\.gitignore$|AGENTS\.md$|README\.md$|LICENSE$|CONTRIBUTING\.md$)
# scope-19 is identical to scope-18: Sprint 19 introduces no new src/
# directory — Parley.Configuration lives in src/exec/, which
# scripts/run-tests.st already files in — so that script is unchanged
# and must not be edited.
#
# Declared settled-class exceptions this sprint are FOUR:
#
#   Publisher   - the stdout redirect on its composed plumbing command
#     lines (mkdir, ln, rm, cd && zip). Nothing about what it writes.
#   Installer   - the same, for its one /usr/bin/install -c line.
#   CommandLine - consult Configuration when a source flag is absent,
#     and the new parley.config.st boundary-table row with its declared
#     sentence. PRECEDENCE LIVES HERE and nowhere else.
#   ManifestFile - load: answers the anchor diagnosis when an anchor was
#     recovered, regardless of what recorded holds (§8 decision 76).
#
# NOT exceptions, each a halt-and-ask: ProcessRunner (decision 63 is a
# redirect on a COMPOSED LINE, not a change to the seam - the seam still
# captures nothing), ExecutionScope (exec is carved out: its plumbing and
# the author's program share one stderr, so separating them means
# buffering, which roadmap §3 defers by name), CLI (it already passes the
# flags through), IndexEntryReader (a new schema tag is a reader CONSUMER,
# not a reader change), Resolver/IndexSnapshot, PathGuard and
# ManifestCapture.
#
# THE CENSUS STAYS AT EIGHT. The parley.config.st read routes through
# PathGuard like every other boundary; if Configuration's source acquires
# 'FileStream ', 'File name:', 'Directory create:' or 'Directory
# workingName' it becomes a ninth class and Sprint17AcceptanceTest>>testS13
# fails by construction (§8 decisions 60, 65).
#
# bin/ stays in scope - the shipped binary is law-guarded from Sprint 8
# onward, and this sprint's chatter laws spawn it to capture stdout AND
# stderr separately, reusing Sprint 18's runner rather than writing a
# second one.
#
# docs/method/ remains operator-authored and is NOT agent-writable, as
# scope-15 through scope-18 record.
scope-19: ^(src/compat/|src/domain/|src/manifest/|src/resolver/|src/source/|src/install/|src/exec/|src/publish/|bin/|tests/|scripts/|docs/|\.parley/|\.githooks/|\.github/|\.gitignore$|AGENTS\.md$|README\.md$|LICENSE$|CONTRIBUTING\.md$)
# scope-20 is identical to scope-19: Sprint 20 introduces NO NEW CLASS and
# no new src/ directory — the four verbs live in the settled CLI, deletion
# lives in the settled ManifestEditor (src/manifest/) — so
# scripts/run-tests.st is unchanged and must not be edited.
#
# Issue #22. Specs of record: Doc E §4, §4.1, §4.2; Doc B §2.1; Doc C §2;
# Doc F §7.3. Decisions 67, 79 and 80 landed BEFORE red at 9b5d443, with
# the doc changes citing them — the spec you read is already correct.
#
# Declared settled-class exceptions this sprint are FIVE, each bounded:
#
#   ManifestEditor  - gains the deletion recognizer and its splice. It
#     recognizes the clause AND THE SEPARATOR binding it to its neighbours
#     (§8 decision 67): a deletion that recognizes too little DAMAGES where
#     an insertion that recognizes too little merely refuses. The insertion
#     path is untouched.
#   CLI             - four verb rows in verbRows and their dispatch; the
#     usage composition split into BLOCK and REASON+BLOCK; and info
#     SENDING THE SETTLED PRIVATE seededSourceAdding: — add's own,
#     written for the identical reason (a name in neither Package.st nor
#     parley.lock must be seeded explicitly, issue #16 S16). No new
#     mechanism; that private's method comment gains its second-caller
#     sentence, which is inside this bound. Existing verbs unchanged.
#   CommandLine     - four usage rows; helpResult STOPS taking usageResult
#     lines wholesale (§8 decision 79) and its settled method comment
#     recording that coupling is rewritten; the search-capability predicate
#     keyed off the settled sourceFlags declaration.
#   IndexSnapshot   - TWO PURELY ADDITIVE ACCESSORS ONLY: a sorted package-
#     name enumeration, and an all-versions-INCLUDING-RETIRED enumeration
#     deliberately distinct from versionsOf:'s decision-37 filtered view.
#     No existing selector changes behaviour and the Resolver calls
#     neither. This is src/resolver/ — expect these two methods to be the
#     sprint's most-reviewed lines.
#   GitIndexSource  - refreshCommand's TWO COMPOSED LINES GAIN --quiet,
#     nothing else (the F19 carried gap; §8 decision 77 applied to the
#     domain member that finding named). The scan, the fetch:version:
#     composition, the cache keying by Sha256 and the failure-diagnosis
#     composition are untouched.
#
# GitIndexSource is on this list because Sprint 19's lineage above teaches
# it in mirror: that block named Installer as an exception and the pre-RED
# ruling withdrew it (Installer composes no command line at all), while
# ExecutionScope and IndexEntryReader had to be GRANTED mid-flight. A rider
# that edits settled code is declared here or it is a halt-and-ask.
#
# NOT exceptions, each a halt-and-ask: ProcessRunner (--quiet is a flag on
# a COMPOSED LINE, never a change to the seam), Resolver and every strategy
# (§8 decision 40 — the read-only verbs are proved by trap double never to
# enter it), DirectorySource and SparseIndexSource (search's capability is
# keyed off sourceFlags, NOT a new PackageSource protocol selector — if a
# protocol method looks genuinely cleaner, HALT and say so on #22),
# Publisher, PathGuard, ManifestBuilder, ManifestCapture, ManifestFile.
#
# NO SCHEMA MOVES. #'parley-index' 1 is byte-identical; §8 decision 80
# REFUSES the sparse catalogue rather than building it.
#
# DECLARED PASSES-IN-RED: NONE. A sprint adding verbs and laws over new
# declarations should have nothing passing before its code exists, so any
# pass in red is undeclared by definition and is a finding at Gate A.
#
# bin/ stays in scope - the shipped binary is law-guarded from Sprint 8
# onward, and S20-S22 spawn it to capture stdout AND stderr separately on
# a --git verb, which NO existing law does. That hole is exactly why the
# F19 gap shipped.
#
# docs/method/ remains operator-authored and is NOT agent-writable, as
# scope-15 through scope-19 record.
scope-20: ^(src/compat/|src/domain/|src/manifest/|src/resolver/|src/source/|src/install/|src/exec/|src/publish/|bin/|tests/|scripts/|docs/|\.parley/|\.githooks/|\.github/|\.gitignore$|AGENTS\.md$|README\.md$|LICENSE$|CONTRIBUTING\.md$)
# scope-21 is identical to scope-20: Sprint 21 introduces TWO NEW CLASSES —
# Parley.PathOverlay (src/source/) and Parley.PathDependency (src/domain/) —
# but NO new src/ directory: both land in directories scripts/run-tests.st
# already files in, so that script is unchanged and must not be edited.
#
# Issue #23. Specs of record: Doc B §2/§3.2/§3.4/§4/§5.2/§5.3; Doc C
# §1.2/§1.3/§2/§7; Doc E §4/§4.1/§4.2/§5; Doc F §1 step 0. Decisions 68, 69
# and 83 landed BEFORE red at c86a754, with the doc changes citing them —
# the spec you read is already correct.
#
# Declared settled-class exceptions this sprint are TEN, each bounded (the
# full bounds live in issue #23's table — the issue is the contract):
#
#   ManifestBuilder  - the two vocabulary methods and their raw recording;
#     build validation per Doc B §3.4 items 4-6. DNU path untouched.
#   LibraryManifest  - pathDependencies and devDependencies (collections,
#     accessors, construction); fromIndexEntry: reconstructs neither.
#   Publisher        - Doc F §1 step 0, the path-dependency pre-flight
#     refusal. Nothing else.
#   CLI              - verbRows: resolve's guard drops its source
#     requirement (§8 decision 83) — ONE guard, no other row; resolve's
#     three states and overlay construction; the conflict-provenance line;
#     the lock write rendering #path tuples and the path-free view handed
#     to Installer; exec's sibling file-in input; the matrix legs of
#     info/search/outdated/why/check; --locked at the lock-write site;
#     usageBlock header + two flags rows. THE HOLDING MAP EXCLUDES
#     PATH-PROVENANCE NAMES (§8 decision 68) — a path pin is never held.
#   CommandLine      - the mode-flag declared table and extraction; the
#     --offline wiring refusal keyed off the settled sourceFlags; handing
#     the modes to the CLI; the two declared refusal wordings. sourceFlags
#     itself UNTOUCHED — SourceFlagTest:149 must not move.
#   IndexSnapshot    - the additive overlay composition with the provenance
#     split (Doc C §2). src/resolver/ — most-reviewed lines.
#   Resolution       - #path provenance: fromLockEntry:, a provenance
#     accessor, the path-free view. src/resolver/.
#   PinVerification  - the three-kinds extension. Existing wordings
#     byte-identical. src/resolver/.
#   IndexEntryWriter - writeLock:on: renders the #path tuple; a path-free
#     lock's bytes IDENTICAL to Sprint 20's. Entry side untouched.
#   ExecutionScope   - the child composition gains the path file-ins;
#     unchanged when no path pins; register untouched.
#
# NOT exceptions, each a halt-and-ask on #23: ProcessRunner, Resolver and
# every strategy and ConstraintLedger (§8 decision 40), DirectorySource,
# GitIndexSource and SparseIndexSource (the overlay is a DECORATOR and the
# re-seed rides the settled seededWith: — if a source change looks cleaner,
# HALT), Installer/InstalledSet/ContentStore (the path-free view exists so
# these never learn paths exist), ManifestFile, ManifestCapture,
# ManifestEditor, PathGuard, Configuration, Sha256, Dependency.
#
# NO SCHEMA MOVES for entries: #'parley-index' 1 is byte-identical (§8
# decision 69). The LOCK gains one declared alternative pin-tuple form,
# #(<name> <version> #path '<declared path>') — tag, format version and
# both keys unchanged; a path-free lock is byte-identical to Sprint 20's.
#
# DECLARED PASSES-IN-RED: EXACTLY ONE — S33's bound guard (a source-less
# install still answers the #needsIndex usage error; §8 decision 83 defers
# the mirror). Any OTHER pass in red is undeclared by definition and is a
# finding at Gate A. Write NO dedicated no-overlay regression law: the 983
# settled laws are that guard.
#
# THE CENSUS STAYS AT EIGHT. PathOverlay performs no direct File I/O —
# siblings are read through the settled ManifestFile. If PathOverlay's
# source acquires 'FileStream ', 'File name:', 'Directory create:' or
# 'Directory workingName' it becomes a ninth class and
# Sprint17AcceptanceTest>>testS13 fails by construction (§8 decision 60).
#
# bin/ stays in scope — the shipped binary is law-guarded from Sprint 8
# onward, and this sprint's end-to-end workflow scenario (S10) spawns it.
#
# docs/method/ remains operator-authored and is NOT agent-writable, as
# scope-15 through scope-20 record.
scope-21: ^(src/compat/|src/domain/|src/manifest/|src/resolver/|src/source/|src/install/|src/exec/|src/publish/|bin/|tests/|scripts/|docs/|\.parley/|\.githooks/|\.github/|\.gitignore$|AGENTS\.md$|README\.md$|LICENSE$|CONTRIBUTING\.md$)
# scope-22 GROWS for the first time since Sprint 8: the release sprint
# creates root-level artifacts no prior regex admitted — CHANGELOG.md,
# SECURITY.md, CODE_OF_CONDUCT.md, the root Package.st (§8 decision 86,
# the package-of-Parley), and the completions/ directory (generated from
# the declared tables). NO new src/ directory and NO new class:
# scripts/run-tests.st is unchanged and must not be edited.
#
# Issue #24. Specs of record: §8 decisions 70, 71, 85, 86, 87 (landed
# BEFORE red at 27f68c8 with the doc changes citing them — the spec you
# read is already correct); Doc E §4.1/§4.2/§4.3/§5; Doc B §4 + scope
# line; Doc F §1 step 3; roadmap §3's two amended rows.
#
# Declared settled-class exceptions, each bounded (full bounds in issue
# #24's table — the issue is the contract):
#
#   Parley.Parley - releaseFiles beside the settled version, 'release'
#     category. Nothing else.
#   CommandLine   - the env read at wiring (ONE injectable seam; default
#     Smalltalk getenv:), the per-row PARLEY_* field on the source-flag
#     declaration, the two-variables refusal wording, flag > env > file
#     at the existing precedence site. Mode flags and usage lines
#     untouched.
#   CLI           - needsIndexRefusalFor: gains the variables clause from
#     the declared table; exec:'s script path resolved against the
#     WORKING DIRECTORY (the :894 precondition and the path handed to
#     ExecutionScope). verbRows and usageBlock DO NOT MOVE.
#   Publisher     - stage creates each fileIn's parent directories via
#     PathGuard, preserving relative paths (Doc F §1 step 3).
#
# Settled TEST files that move: CliTest (S18 exec family re-pins for the
# workdir-resolved path); PinVerificationTest and Sprint3AcceptanceTest
# each LOSE TWO LAWS (the ApplicationManifest deletion, §8 decision 87 —
# the count moves DOWN 4 before this sprint's laws move it up);
# SourceFlagTest:149 re-pins (the declared row grows the PARLEY_* field);
# UsageGroundTest and Sprint19AcceptanceTest re-pin the needs-index
# sentence; the README-pinning families re-pin for the restructure.
#
# NOT exceptions, each a halt-and-ask on #24: Configuration (§8 decision
# 85 keeps it the FILE reader — its non-movement is a checkable claim),
# ExecutionScope, Resolver and every strategy and ConstraintLedger,
# PathOverlay, PathDependency, IndexSnapshot, Resolution, PinVerification
# (the production class — only its test file moves), all three sources,
# Installer/InstalledSet/ContentStore, ProcessRunner, ManifestBuilder,
# LibraryManifest, ManifestFile, ManifestCapture, ManifestEditor,
# PathGuard, Sha256, IndexEntryWriter/Reader, SchemaShape.
#
# THE ARGV GRAMMAR DOES NOT MOVE: no new verb, no new flag; every new
# refusal is in-verb at exit 1 (decisions 80/83). The env layer's suite
# is IMMUNE to the harness process environment: every law pins its env
# through the seam; an exported PARLEY_SOURCE on the operator's machine
# must not change a single outcome.
#
# DECLARED PASSES-IN-RED: EXACTLY THREE — S24 (the CI drift law: the
# workflow has invoked the one gate since H0), S25 (row 15: the wrap
# staging list vs THIS regex, over existing files), S26 (the
# docs/sprints/README.md drift law: the index was trued at the Sprint 21
# close-out). Derived fresh per law — the plan's pre-brainstorm "none"
# row is superseded. Any OTHER pass in red is undeclared by definition
# and is a finding at Gate A.
#
# THE CENSUS STAYS AT EIGHT: Publisher already holds its seat and the
# stage's parent creation routes through PathGuard.
# deserializationBoundaries does not grow. The §4.2 error partition does
# not grow.
#
# The dogfooding hygiene is already committed (.gitignore): the
# checkout's .parley/ holds the harness's TRACKED scope and UNTRACKED
# audit beside the runtime state a root Package.st makes possible — the
# reset gesture inside THIS checkout is the three state subdirectories,
# never .parley whole (§8 decision 86).
#
# bin/ stays in scope — the shipped binary is law-guarded, and S12's
# showcase loop and S13's cwd contrast spawn it.
#
# docs/method/ remains operator-authored and is NOT agent-writable, as
# scope-15 through scope-21 record.
scope-22: ^(src/compat/|src/domain/|src/manifest/|src/resolver/|src/source/|src/install/|src/exec/|src/publish/|bin/|tests/|scripts/|docs/|\.parley/|\.githooks/|\.github/|\.gitignore$|AGENTS\.md$|README\.md$|LICENSE$|CONTRIBUTING\.md$|CHANGELOG\.md$|SECURITY\.md$|CODE_OF_CONDUCT\.md$|Package\.st$|completions/)

# scope-23 is scope-22 VERBATIM — the first unchanged regex since Sprint
# 21, and it is checked rather than assumed: docs/ already admits the new
# docs/guide/ tree, tests/ admits the new law and fixture files,
# CONTRIBUTING.md is already named, and the sprint adds NO src/ directory
# and NO class. scripts/run-tests.st is therefore unchanged and must not
# be edited.
#
# Issue #25. Specs of record: §8 decisions 88 (a diagram is a declaration
# and the law states what it cannot prove), 89 (the README is
# first-contact; a claim that moves takes its law with it) and 90 (§8 is
# never relocated; it gains a lawed index) — all three landed BEFORE red
# with the doc changes citing them, so the spec you read is already
# correct. Beside them: decision 70 (a growing set rides as the file that
# declares it), decision 43 (a law states its own textual bound), and
# finding F19 (enumerate a rule's domain in the units the RULE quantifies
# over).
#
# THE TOOL'S BEHAVIOUR DOES NOT CHANGE THIS SPRINT. No verb, no flag, no
# diagnosis, no exit code, no serialized byte moves. If an implementation
# step would change what the tool does, that is the signal to halt.
#
# Declared settled-class exceptions, each bounded (full bounds in issue
# #25 — the issue is the contract):
#
#   Parley.Parley - releaseFiles gains EXACTLY ONE entry,
#     'docs/guide/README.md', and it lands as a SIXTH Array group:
#     3.2.5's Array class answers no with: longer than five, which is why
#     the declaration is already five groups of five. Nothing else in the
#     method, and nothing else in the file — `version` in particular does
#     NOT move.
#
# Settled TEST files that move, and the ONLY two:
#
#   ReleaseHardeningTest - exactly two selectors,
#     testTheReadmeCarriesTheExecConsumerConstraint and
#     testTheReadmeDocumentsPublishingIntoBothLayouts, each re-pointed to
#     its docs/guide/ page AND STRENGTHENED. Re-pointing alone is the
#     defect: two of the layout law's three markers are satisfied today
#     by a marketing sentence at README:13 that documents nothing (S4).
#   PublishLayoutFixtures - the two marker selectors above, plus the
#     page-source readers the re-pointed laws need.
#
# NOT movers, and each must still pass over the trimmed README (S5, S6):
# ReleaseDriftTest's three laws and Sprint22AcceptanceTest S27-S30 keep
# their current subjects while the subject is rewritten underneath them.
#
# THE VERSION DOES NOT MOVE. Parley version, the Package.st literal and
# CHANGELOG.md all stay 1.0.0 and are not to be touched: the heading-set
# law asserts the released set is exactly one member, so a bump breaks a
# settled release law rather than adding a section. A docs increment is
# not a release.
#
# THE DIAGRAMS AND THE §8 INDEX ARE GREEN DELIVERABLES, never pre-RED doc
# amendments. A diagram that arrives before its law makes the law pass on
# arrival and proves nothing. The assertion that fails on today's
# diagramless tree is the CENSUS (S12); write it red first.
#
# DECLARED PASSES-IN-RED: EXACTLY TWO — S5 (the untrimmed README already
# names every verb) and S6 (it already satisfies the slots law, the tally
# equality and S27/S28/S30). Both are regression guards over artefacts
# that already conform. THE LAW-COUNT LAW IS NOT AMONG THEM: it compares
# the README's number against the DISCOVERED suite, so it goes red the
# moment RED's new test files load and returns green only when the
# sentence is rewritten at GREEN's end. Any OTHER pass in red is
# undeclared by definition and is a finding at Gate A.
#
# NOT exceptions, each a halt-and-ask on #25: Configuration, CommandLine,
# CLI, Publisher, ExecutionScope, Resolver and every strategy,
# ConstraintLedger, PathOverlay, PathDependency, IndexSnapshot,
# Resolution, PinVerification, all three sources,
# Installer/InstalledSet/ContentStore, ProcessRunner, ManifestBuilder,
# LibraryManifest, ManifestFile, ManifestCapture, ManifestEditor,
# PathGuard, Sha256, IndexEntryWriter/Reader, SchemaShape.
#
# docs/method/ remains operator-authored and is NOT agent-writable, as
# scope-15 through scope-22 record. F39 is owed and is the operator's to
# write; the tally sentence in README.md moves only with findings.md, in
# one edit, and neither moves this sprint.
scope-23: ^(src/compat/|src/domain/|src/manifest/|src/resolver/|src/source/|src/install/|src/exec/|src/publish/|bin/|tests/|scripts/|docs/|\.parley/|\.githooks/|\.github/|\.gitignore$|AGENTS\.md$|README\.md$|LICENSE$|CONTRIBUTING\.md$|CHANGELOG\.md$|SECURITY\.md$|CODE_OF_CONDUCT\.md$|Package\.st$|completions/)
